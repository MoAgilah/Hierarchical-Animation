#ifndef AnimationParserH
#define AnimationParserH

#include "Application.h"
#include <vector>

using namespace std;

struct anim
{
	string name;
	string transName;
	vector<float> kframe;
	vector<float> val;
};

//a animation parser for all animations
__declspec(align(16)) class AnimationParser
{
public:
	AnimationParser();
	~AnimationParser();
	bool LoadFromFile(const string& fName, const string& aName);
	vector<anim> PrepAnimationData(const float& scaler = 1.0f);
private:
	vector<string> animData;
	vector<anim> animation;
public:
	void* operator new(size_t i)
	{
		return _mm_malloc(i, 16);
	}

		void operator delete(void* p)
	{
		_mm_free(p);
	}
};

#endif

