#ifndef BulletPoolH
#define BulletPoolH

#include "Bullet.h"
#include <vector>
#include <assert.h>

#define MaxBullet 100

class BulletPool
{
public:
	BulletPool();
	~BulletPool();
	void Create(const XMMATRIX& gunMatrix, const float& iVel);
	void Update(const float& deltaTime);
	void Draw();
private:
	std::vector<Bullet*> bullets;
	Bullet* firstAvailable;
	CommonMesh* p_Bullet;
};

#endif