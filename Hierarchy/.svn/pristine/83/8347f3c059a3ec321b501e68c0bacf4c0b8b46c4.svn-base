#ifndef AnimationH
#define AnimationH

#include "Application.h"
#include "AnimationParser.h"
#include "MeshComponent.h"
#include <string>
#include <vector>

using namespace std;

//animation clip
__declspec(align(16)) class Animation
{
public:
	Animation(const string& fName, const string& afName, const double& scalar = 1.0f);
	~Animation();
	void SetKeyFrames();
	vector<anim> GetFrameTrans();
	vector<float> GetKeyframes();
private:
	string name;
	vector<anim> ani;
	vector<float> keyframes;
public:
	void* operator new(size_t i)
	{
		return _mm_malloc(i, 16);
	}

		void operator delete(void* p)
	{
		_mm_free(p);
	}
};

#endif

